# Version 2: Avec Node.js explicite
services:
  - type: web
    name: smartcity-test-v2
    env: node
    buildCommand: npm install && npm run build
    startCommand: npx serve -s dist -l 3000
    envVars:
      - key: NODE_VERSION
        value: 18
